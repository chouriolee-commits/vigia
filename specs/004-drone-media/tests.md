# 004 — Drone Media — Tests

## Críticos (MVP)
- `LiveFeedPanel` renderiza overlays de detección según el mock.
- `useLiveFeed` avanza el timestamp simulado (fake timers).
- Click en "Revisar imágenes capturadas" no produce error ni navegación rota.

## Opcionales / fase futura
- Tests de galería y modal de detalle (solo si esa fase se implementa).
