# 010 — Testing — Tasks

## MVP
- [ ] Verificar que `npm run test` pasa para los tests críticos ya definidos en cada `tests.md` de dominio (marcados "Críticos (MVP)").
- [ ] Configurar Playwright mínimamente y escribir **un** flujo E2E (`dashboard-navigation.spec.js`) que cubra los 3 botones + volver.

## Fase futura
- [ ] Separar flujos E2E adicionales por archivo (alertas, eventos, chat IA, detección).
- [ ] Pytest + HTTPX para endpoints reales (si `009-api-integration` se implementó).
